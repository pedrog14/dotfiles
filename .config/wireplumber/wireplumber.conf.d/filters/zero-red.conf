{
  node.name = filter.sink.eq
  node.description = "EQ Sink"
  filter.graph = {
    nodes = [
      {
        type = builtin
        name = eq
        label = param_eq
        config = {
          filename = "/home/pedrog/.config/wireplumber/wireplumber.conf.d/filters/eq/Truthear x Crinacle Zero_RED Filters.txt"
        }
      }
    ]
  }
  capture.props = {
    node.name = "EQ Sink Input"
    media.class = Audio/Sink
    filter.smart = true
    filter.smart.name = filter.sink.eq
  }
  playback.props = {
    node.name = "EQ Sink Output"
    node.passive = true
  }
}
